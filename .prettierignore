node_modules
dist
dist-ssr
.reference

# Lockfiles are generated — reformatting them creates churn and risks corruption
pnpm-lock.yaml
# studio/ is a separate install that still uses npm — see pnpm-workspace.yaml
studio/package-lock.json

# Sanity Studio — generated / installed locally
studio/node_modules
studio/dist
studio/.sanity
